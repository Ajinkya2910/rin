% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{install}
\alias{install}
\title{Install R packages with rin}
\usage{
install(
  packages = character(),
  retry = FALSE,
  skip_sysreq = FALSE,
  ignore_missing = character(),
  strict_sysreq = FALSE
)
}
\arguments{
\item{packages}{Character vector of package names (CRAN or Bioconductor).
May be omitted when \code{retry = TRUE}.}

\item{retry}{Resume a previously failed install instead of starting fresh.}

\item{skip_sysreq}{Skip the system-requirements pre-flight entirely.}

\item{ignore_missing}{Character vector of system libraries to ignore in the
audit (maps to repeated \code{--ignore-missing}).}

\item{strict_sysreq}{Block the install when sysreqs appear missing
(default is advisory).}
}
\value{
The integer exit status of the rin process, invisibly. \code{0} on
success.
}
\description{
Wraps \verb{rin install}. Runs rin's pre-flight system-dependency audit and
parallel install, streaming output to the console. On success the install
target library is added to \code{.libPaths()} so the packages are immediately
usable in the current session.
}
\examples{
\dontrun{
rin::install("DESeq2")
rin::install(c("ggplot2", "dplyr"))
rin::install("gert", ignore_missing = "libgit2-dev")
rin::install(retry = TRUE)
}
}
