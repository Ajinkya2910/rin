% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/restore.R
\name{restore}
\alias{restore}
\title{Restore the whole project from rin.lock}
\usage{
restore()
}
\value{
The integer exit status of the rin process, invisibly. \code{0} on success.
}
\description{
Wraps \verb{rin restore} — installs and repairs \strong{every} package recorded in the
project's \code{rin.lock}, not just one. Use it to rebuild a project's full
environment: on a new machine, after \verb{git clone}, or after fixing a broken
system dependency. On success the install library is added to \code{.libPaths()}
so the packages are usable in the current session.
}
\details{
This is the project-wide counterpart to \code{\link[=install]{install()}}: \code{install()} adds/installs
a single package (and fails only if that package fails), whereas \code{restore()}
builds the entire locked project (and fails if anything in it fails).
}
\examples{
\dontrun{
rin::restore()
}
}
\seealso{
\code{\link[=install]{install()}}
}
