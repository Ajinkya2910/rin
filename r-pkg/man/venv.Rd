% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/venv.R
\name{venv}
\alias{venv}
\title{Create or manage a project-local R environment with rin}
\usage{
venv(path = ".rin", r_version = NULL)
}
\arguments{
\item{path}{Name or path for the environment (default \code{.rin}).}

\item{r_version}{Optional R version string (maps to \code{--r-version}).}
}
\value{
The integer exit status of the rin process, invisibly.
}
\description{
Wraps \verb{rin venv}. Creates a project-local library plus an \code{activate}
script. Note that activation itself is a shell concept (it exports
\code{R_LIBS_USER}); a GUI RStudio launched from the Dock will not pick that up
automatically. To use a venv's packages inside the current R session, point
R at its library directly, e.g. \code{.libPaths("<path>/lib")}.
}
\examples{
\dontrun{
rin::venv("myproject")
rin::venv("myproject", r_version = "4.4.0")
}
}
